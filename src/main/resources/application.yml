spring:
  application: name=book-share-app

  profiles:
    active: dev

  servlet:
    multipart:
      max-file-size: 50MB

springdoc:
  default-produces-media-type: application/json

server:
  servlet:
    context-path: /api/v1/

logging:
  level:
    org.springframework.web: DEBUG
